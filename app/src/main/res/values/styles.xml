<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorToolbar</item>
        <item name="colorPrimaryDark">@color/colorStatusBar</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="toolbarStyle">@style/ClubToolbar</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- 滑动返回 背景样式 -->
    <style name="BaseAppTheme" parent="AppTheme">
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="BaseAppTheme.Light" parent="Theme.AppCompat.Light">
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- 浅色主题（状态栏浅色文字 深色背景） -->
    <style name="BaseAppTheme.Light.NoTitleBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">#007FFF</item>
        <item name="android:windowTranslucentStatus" tools:targetApi="19">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="toolbarStyle">@style/ClubToolbar</item>
    </style>

    <!--解决Toolbar左边空白的属性-->
    <style name="ClubToolbar" parent="Widget.AppCompat.Toolbar">
        <item name="contentInsetStart">0dp</item><!-- 设置该属性解决空白部分-->
    </style>

</resources>
